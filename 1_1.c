#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#define MAX_SIZE 128
#define SWAP(x,y,temp) (temp=x, x=y, y=temp)

int main(void)
{
	FILE* fp = fopen("lab1_1test1.txt","r")

	int i, n;
	int original[n];
	
	fclose(fp);
	return 0;
}